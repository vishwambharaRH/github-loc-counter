[package]
name = "loc_runner"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "loc_runner"
path = "loc_runner.rs"

[dependencies]
walkdir = "2.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"